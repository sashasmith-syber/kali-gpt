import React from 'react'
import ReactDOM from 'react-dom/client'
import KaliGptClaude from './kali-gpt-claude.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <KaliGptClaude />
  </React.StrictMode>,
)
